#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int geranumero (long u, int i, int s);
long semente ();
char *resposta (int i, int s);
void soma (int dificuldade);
void subtrac (int dificuldade);
void multiplica (int dificuldade);
void divide (int dificuldade);